# Introdução à programação Python

## 223-[PF]-KC - Python para administração do sistema

#### 1. Qual função é equivalente à função defasada `os.system()` no Python V3?
- [ ] open()
- [x] subprocess.run()
- [ ] os.uname()
- [ ] json.load()

***

#### 2. Qual destes é um objetivo da administração do sistema?
- [ ] Acompanhar versões do código fonte
- [ ] Encontrar defeitos nos aplicativos
- [x] Garantir a estabilidade do sistema
- [ ] Projetar os esquemas de banco de dados

***

#### 3. Qual parte atualiza os pacotes no sistema operacional atual?
def update_environment():
    os.system("sudo apt-get update")
    os.system("sudo apt-get upgrade")
    os.system("sudo apt-get dist-upgrade")

- [ ] `apt-get update`  
- [x] `apt-get upgrade`  
- [ ] `apt-get dist-upgrade`  
- [ ] `update_environment()`

***

#### 4. Qual destas é uma tarefa comum para administradores de sistema (SysAdmins)?
- [x] Instalação de novo hardware ou software  
- [ ] Criação de um framework de solução de nuvem para migrar uma carga de trabalho para a nuvem  
- [ ] Resolver problemas de conectividade da rede entre o data center da empresa e o escritório corporativo  
- [ ] Desenvolver e implantar patches durante janelas de manutenção

***

#### O que o código faz?
def new_user():
    confirm = "N"
    while confirm != "Y":
        username = str(input("Enter the name of the user: "))
        print("Use the username '" + username + "'? (Y/N)")
        confirm = str(input("")).upper()
        os.system("sudo adduser " + username)

- [ ] Converte o valor de `username` para letras minúsculas.
- [ ] Adiciona um nome de usuário. 
- [ ] Cria um nome de usuário.
- [x] Adicionando um usuário

***