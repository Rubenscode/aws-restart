# Linux

## 252-[LX]-KC - Scripts de shell do Bash

#### 1. Por que um administrador pode criar um shell script do Bash? (Selecione DUAS respostas.)
- [x] Para garantir que uma tarefa seja executada de forma correta e consistente.
- [ ] Para usar o recurso de validação de dados incorporado do shell Bash ao executar uma tarefa.
- [ ] Para documentar comandos comumente usados em um único arquivo.
- [ ] Para executar uma tarefa que requer permissões elevadas.
- [x] Para automatizar uma tarefa repetitiva.

***

#### 2. Qual das seguintes opções exibe a sintaxe correta para a primeira linha de um script bash para torná-lo executável?
- [x] `#!/bin/bash`
- [ ] `#!/bin`
- [ ] `#!/bash`
- [ ] `#!`

***

#### 3. Considere a seguinte declaração: `sum=$(($2 + $4))`. Quais partes da declaração representam os argumentos?
- [ ] `$ e () são os argumentos.`
- [ ] `sum e ($2 + $4) são os argumentos.`
- [x] `$2 e $4 são os argumentos.`
- [ ] `$() e ($2 + $4) são os argumentos.`

***

#### 4. Qual comando faz com que um script Bash pare de ser executado e saia do shell?
- [ ] `wait` (aguardar)
- [x] `exit` (sair)
- [ ] `kill` (encerrar)
- [ ] `return` (retornar)

***

#### 5. Qual declaração condicional define dois cursos de ação?
- [ ] `if` (se)
- [ ] `while` (durante)
- [ ] `for` (para)
- [x] `if - else` (se, então)

***
